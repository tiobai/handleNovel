const component = () => {
  const footer = document.createElement("div");
  footer.innerText = `E-mail: yesangchun@gmail.com`;
  footer.classList.add("footer");
  return footer;
};

export default component;
